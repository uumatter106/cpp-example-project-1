A project for about C++ things. (Learning... C++).

If you want to build&run the revision examples(REX) then open 'buildrun.ps1' and read.
If you understand how then use it.

The project only used and developed in Windows.
.. So you're not guarenteed to use powershell files in other OSes.
